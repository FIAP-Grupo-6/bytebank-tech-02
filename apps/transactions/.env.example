# URL da API do Tech Challenge 2
NEXT_PUBLIC_API_URL=http://localhost:3000

# DEVE ser IDÊNTICO ao do host — é o que permite a zona decodificar
# o cookie de sessão do NextAuth (SSO entre microfrontends)
NEXTAUTH_SECRET=bytebank-dev-secret-troque-em-producao
